#1.7 Keyworded Arguments : 인자의 순서를 신경 쓸 필요가 없다.

def plus(a, b):
    return a - b

#result = plus(b=30, a=1)
#print(result)

#1.8 Code Challenge!
def plus(a, b):
    result=int(a)+int(b)
    return result

print(plus(12,"10"))
